import * as Phaser from 'phaser';

/**
 * BattleUIScene — full implementation handled by Task #3.
 * Runs as an overlay on top of BattleScene.
 */
export class BattleUIScene extends Phaser.Scene {
  constructor() {
    super({ key: 'BattleUIScene' });
  }

  create(): void {
    // Placeholder — battle UI will be implemented in Task #3
  }
}
